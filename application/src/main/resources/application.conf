{
    app: {
        version: "${project.version}",
        name: "${app.name}"
    },

    vcap: {
        services: "{}",
        services: ${?VCAP_SERVICES},
        application: "{}",
        application: ${?VCAP_APPLICATION}
    }
}
